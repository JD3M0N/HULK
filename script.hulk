print(2 + "si");

function fact(n) => if (n <= 1) 1 else n * fact(n - 1 * 3 * z);

function fib(n) => if (n <= 1) n else fib(n - 1) + fib(n + m - 2, 4 + "se");

function sum(n) => if (n <= 0) 0 else n + sum(n - 1);

function power(base, exp) => if (exp <= 0) 1 else base * power(base, exp - 1);

print(fact(5));
print(fib(6));
print(sum(10));
print(power(2, 4));