type A {
  foo() => 5;
}

type B inherits A {
  foo() => 10;
}

function bar(x) => {
  if (x>5) {
    new A();
  } else {
    new B();
  };
};

function g(x, y) => {
    if (y < 5) {
        x;
    } else {
        "hola";
    }; 
};

let a  = new A() in print(a.foo());