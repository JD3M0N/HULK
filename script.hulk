type A {
  foo() => 5;
}

type B inherits A {
  foo() => 10;
}

function bar(x) => {
  if (x>5) {
    new A();
  } else {
    new B();
  };
};

function g(x, y) => {
    if (y < 5) {
        x;
    } else {
        "hola";
    }; 
};

function f(a: Number, b): Number {
    print(a + b);
}

type Point {
    x: Number = 0;
    y: Number = 0;

    getX() => self.x;
}

let pt = new Point() in print(pt.getX());